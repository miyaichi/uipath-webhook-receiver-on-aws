{
  "name": "uipath-webhook-receiver",
  "description": "Receives Webhook and orders Orchestrator to start the job",
  "version": "0.1.0",
  "dependencies": {
    "serverless-python-requirements": "^4.2.5"
  },
  "devDependencies": {
    "serverless-python-requirements": "^4.2.5"
  }
}
